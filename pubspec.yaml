name: cryptocurrency_flutter
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.3
  google_fonts: 2.1.0
  nb_utils: ^4.3.0+6

  #Firebase
  firebase_core: ^1.3.0
  firebase_analytics: ^8.1.2
  #firebase_crashlytics: ^2.0.7
  google_mobile_ads: ^0.13.2
  onesignal_flutter: ^3.0.0

  # AppAnimations and functionality
  animated_bottom_navigation_bar: ^0.3.2
  shimmer: ^2.0.0
  lottie: ^1.0.1
  cached_network_image: ^3.0.0
  number_slide_animation: ^0.2.1
  readmore: ^2.1.0


  #Charts
  chart_sparkline: ^1.0.5
  syncfusion_flutter_charts: ^19.2.44+1

  # Storage
  sqflite: ^2.0.0+3

  #API calling
  http: ^0.13.3
  html: ^0.15.0
  flutter_html: ^2.1.0
  intl: ^0.17.0

  # System Info
  package_info_plus: ^1.0.3
  url_launcher: ^6.0.3
  share_plus: ^2.1.4

  # State Management Technique
  mobx: ^2.0.1
  flutter_mobx: ^2.0.0

  flutter_localizations:
    sdk: flutter



dev_dependencies:
  flutter_test:
    sdk: flutter
  # State Management Technique
  build_runner: ^2.0.5
  mobx_codegen: ^2.0.1+3
#flutter packages pub run build_runner build --delete-conflicting-outputs
flutter:

  uses-material-design: true

  assets:
    - images/
    - lang/
    - assets/
    - images/chart_images/
    - images/app_images/
